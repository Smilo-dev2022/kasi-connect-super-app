# Backend (Node/Fastify)
BACKEND_PORT=4000
BACKEND_HOST=0.0.0.0
ENABLE_AUDIT_ENDPOINT=1
AUDIT_LOG_MAX=1000

# Events service (FastAPI)
EVENTS_SECRET_KEY=change-me-in-prod
EVENTS_DATABASE_URL=sqlite:///events.db
EVENTS_BASE_URL=https://events.localhost

# Web (Vite)
VITE_API_ORIGIN=https://api.localhost
