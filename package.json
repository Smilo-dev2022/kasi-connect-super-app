{
  "name": "secure-chat-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "eslint": "^9.12.0",
    "prettier": "^3.3.3",
    "tsconfig-paths": "^4.2.0",
    "tsx": "^4.16.2",
    "turbo": "^2.1.3",
    "typescript": "^5.5.4"
  }
}
